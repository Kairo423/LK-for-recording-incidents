-- init.sql
-- Скрипт инициализации базы данных PostgreSQL.
-- Этот файл выполняется при первом запуске контейнера postgres из docker-compose,
-- если база данных ещё не инициализирована.
-- Добавьте сюда начальную схему или seed-данные при необходимости.


--CREATE TABLE IF NOT EXISTS example (
--    id serial PRIMARY KEY,
--    name text
--);

-- Create users table for testing
